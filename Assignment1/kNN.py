"""
convert the supplied training set to a TF-IDF feature vector
"""
def preprocessor(training_set):
    return feature_vector, labels


def classify(feature_vector, training_labels, new_data):
    return prediction


def run(training_set, test_set):
    feature_vector = preprocessor(training_set)
    for i in test_set:
        p_label.append(classify(feature_vector, i))
        t_label.append()
        # FP? TP? FN? TN?

